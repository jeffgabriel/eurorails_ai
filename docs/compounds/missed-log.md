# Compounds Skill Missed Log

Tracks when the compounds skill was not used or was skipped even though it should have been used.

| Date | Task/Context | Why It Was Missed | What Would Have Helped |
|------|-------------|-------------------|----------------------|
| 2026-02-14 | Fix frontier move distance bonus in Scorer | Plan already identified exact file/function/lines; single-file 5-line edit with known fields on the type | N/A — direct file read was sufficient for this targeted fix |
| 2026-02-22 | Investigate what planRoute() receives — is it enough? | Read systemPrompts.ts, ContextBuilder.ts, LLMStrategyBrain.ts directly without first using compounds | Compounds could have immediately surfaced the old `demandProximityBonus`/`rankDemandChains` geographic clustering (deleted in v6.3) and confirmed it was never replaced in the LLM context. Used compounds after user reminded me. |
| 2026-02-24 | Fix 4 PlanExecutor bugs (advanceStop overshoot, chainArrivalAction false arrival, appendBuildStep money drain, initialBuild PassTurn) | Continued from prior analysis agent session — files already identified; direct targeted edits to PlanExecutor.ts | Bugs were already diagnosed in prior context; all code locations known. Compounds wouldn't have added value over direct file reads for targeted fixes. |
| 2026-02-24 | Gameplay bug investigation: route state not advancing after TurnComposer delivery, speculative builds draining money, Aberdeen bankruptcy | Used Explore agent + direct file reads to trace the TurnComposer→PlanExecutor state disconnect and speculative build fallback. Server logs provided exact turn-by-turn trace. | Compounds search for "TurnComposer route state advancement" or "scanPathOpportunities delivery route sync" could have mapped the data flow gap faster, but server logs + direct reads were effective for this targeted root cause analysis. |
| 2026-02-25 | Investigate LLM call frequency + validate JIRA-5 claims about initial build being heuristic-only | Used Explore agents instead of compounds. Compounds was down (Postgres not running). | Compounds semantic search "planRoute initialBuild decision gate" would have immediately shown that JIRA-1 was already implemented and that `decideAction()` is dead code. Instead, had to dispatch two Explore agents reading full files. JIRA-5 doc was left with stale JIRA-1 dependency claim until user caught it. |
| 2026-02-27 | JIRA-8: Investigate Ruhr track visual gap / phantom connectivity bug | Compounds skill not available in session (unknown skill error). Fell back to grep/glob + direct DB queries. | Compounds search "track segment storage database" and "buildUnionTrackGraph major city internal edges" could have mapped the code graph between TurnExecutor → PlayerService.moveTrainForUser → buildUnionTrackGraph → getMajorCityGroups faster than manual grep chains. |
| 2026-02-27 | TurnComposer multi-target fix: traced A2/A3 MOVE resolution failure through ActionResolver.resolveMove → computeTrackUsageForMove, ContextBuilder.computeCitiesOnNetwork → buildTrackNetwork | Continued from prior compounds-informed investigation; code fix was targeted to known files (TurnComposer.ts). Direct Read/Grep was appropriate for the fix implementation phase. | N/A — compounds was used in prior session for the investigation phase. |
| 2026-03-03 | JIRA-25: Analyze game d8102498 for flash stuck loop, guardrail conflicts, wasted Hirshals ferry track | Analysis was DB-driven (bot_turn_audits, player_tracks queries). Pipeline bugs identified through turn-by-turn DB forensics, not code exploration. | Compounds search "GuardrailEnforcer force pickup drop undeliverable" could have mapped the guardrail interaction graph and shown how Guardrail 4 + 5 create circular overrides. DB analysis was primary, but compounds could have confirmed the code paths faster. |
| 2026-03-03 | Color badges in LeaderboardManager.ts | Small UI change — knew exact file from prior session, single edit to add Phaser circle + shift text. | N/A — targeted single-file edit with known location. Compounds not needed. |
| 2026-03-04 | Bug 1 investigation: Haiku LLM failure chain from T8 onward | Used Explore agents + direct Read/Grep to trace AIStrategyEngine→LLMStrategyBrain→RouteValidator→ActionResolver chain. Compounds skill was available but not invoked. | Compounds search "heuristic fallback discard hand quality" and "RouteValidator cumulative budget" would have mapped the fallback decision tree and validator rejection flow faster than dispatching two parallel Explore agents. |
| 2026-03-10 | Investigate estimatedTurns underestimate for cross-map Cork→Beograd demand | Compounds skill invoked but unavailable (not authenticated). Used direct Read/Grep on ContextBuilder.ts, MapTopology.ts, and game log. | Compounds could have traced the estimateHopDistance→BFS→travelTurns→estimatedTurns data flow and flagged the water-terrain BFS limitation faster. |
| 2026-03-10 | Investigate Haiku LLM failures in game 9126f0b3 | Log analysis task — all data in NDJSON game log file. No code exploration needed. | N/A — this was purely a game log forensics task. Compounds would not have added value since no code architecture questions were involved. |
| 2026-03-10 | Gate upgrade advice behind 3 deliveries + 50M cash | Used Explore agent to find upgrade-related code, then direct Read/Edit. Did not use compounds skill. | Compounds search "upgrade advice serialization prompt" or query "computeUpgradeAdvice" callers would have mapped the full upgrade context flow (computeUpgradeAdvice → checkCanUpgrade → serializePrompt) faster than the Explore agent. |
| 2026-03-10 | Default bot name to model name + Enter key in lobby | Used Explore agent to find lobby UI code. Did not use compounds skill. | Compounds search "BotConfigPopover add bot lobby" would have found the component and its prop chain faster. |
| 2026-03-10 | Fix TS compile errors across 8 test files | Pre-existing type mismatches from interface changes. Used tsc output + direct reads. | N/A — this was mechanical mock-fixing driven by compiler output. Compounds not needed. |
| 2026-03-10 | Game log evaluation + bug investigation (phantom Wheat, A2 chain waste, missing reEvaluateRoute) | Used raw agents reading NDJSON file instead of compounds. The game log analysis itself was data forensics, but when tracing bugs to code (ContextBuilder carried-load state, PlanExecutor A2 chain, TurnExecutor reEvaluateRoute wiring) compounds should have been used. | Compounds search "reEvaluateRoute trigger delivery" and query "ContextBuilder isLoadOnTrain" callers would have mapped the code paths for all three bugs faster than the agent-based approach. |
