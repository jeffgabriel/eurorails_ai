NODE_ENV=development
CLEAN_DB_ON_START=false
PORT=3001
SERVER_DOCKER_PORT=8080

# CORS Configuration
# CLIENT_URL: The primary client application URL (used for CORS and Socket.IO)
# In development, defaults to http://localhost:3000 if not set
# In production, MUST be set to your actual client URL (e.g., https://your-app.com)
CLIENT_URL=http://localhost:3000

# ALLOWED_ORIGINS: Comma-separated list of allowed CORS origins (optional)
# If set, this takes precedence over CLIENT_URL
# Useful for production when multiple domains need access
# Example: ALLOWED_ORIGINS=https://app.example.com,https://www.example.com
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# SECURITY WARNING: In production, always set CLIENT_URL or ALLOWED_ORIGINS explicitly.
# Never use wildcard origins (*) in production without proper authentication.

# Client-Side Configuration (injected at build time via webpack)
# VITE_API_BASE_URL: Base URL for API requests from the client
# In development with webpack proxy, can use relative URLs or http://localhost:3001
# In production, MUST be set to your actual API URL (e.g., https://api.your-app.com)
VITE_API_BASE_URL=http://localhost:3001

# VITE_SOCKET_URL: Socket.IO server URL for real-time connections
# Should match the API server URL in most cases
VITE_SOCKET_URL=http://localhost:3001

# Database Configuration
# 
# Railway/Heroku Support:
# If DATABASE_URL is set, it will be used instead of individual DB_* variables
# Railway automatically provides DATABASE_URL when you link a PostgreSQL service
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://user:password@host:port/database

# Individual database configuration (used if DATABASE_URL is not set)
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eurorails
# Test database name (used when NODE_ENV=test)
# If not set, defaults to 'eurorails_test'
DB_NAME_TEST=eurorails_test
# DB_ENV can be used to select the test database without setting NODE_ENV=test.
# Useful for `npm run dev:test` so the client/webpack still runs in development mode.
# DB_ENV=test
DB_SSL=false
# For Railway PostgreSQL, set DB_SSL=true (or use DATABASE_URL which handles SSL automatically)
DB_MAX_CONNECTIONS=10
DB_IDLE_TIMEOUT=30000 
MIGRATIONS_DIR=db/migrations

# AI Bot Feature Flag
# Set to 'true' to enable AI bot turn execution (default: true)
# Set to 'false' to disable bot turns without removing bots from games
ENABLE_AI_BOTS=true

# Session Configuration
# SESSION_SECRET: Secret key for session encryption (REQUIRED in production)
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=your-very-strong-random-secret-key-here
