#!/bin/bash
# compounds-hooks v3 — installed by 'compounds init-hooks'
# Do not edit manually — re-run 'compounds init-hooks' to update

echo '{"additionalContext": "Tasks generating async (~30s). Follow the flow style from plan_change: in guided or implementation_gate mode, STOP and present the handoff command to the user. In hands_free or planning_gate mode, proceed to implement_all_tasks() without stopping."}'
exit 0
