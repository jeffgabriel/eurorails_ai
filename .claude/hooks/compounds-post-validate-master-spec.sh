#!/bin/bash
# compounds-hooks v3 — installed by 'compounds init-hooks'
# Do not edit manually — re-run 'compounds init-hooks' to update

echo '{"additionalContext": "If validation passed, follow the flow style from plan_change. In guided or planning_gate mode, present the spec to the user for review and approval before proceeding. In implementation_gate or hands_free mode, proceed without waiting for user approval."}'
exit 0
