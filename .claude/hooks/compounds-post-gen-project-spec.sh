#!/bin/bash
# compounds-hooks v3 — installed by 'compounds init-hooks'
# Do not edit manually — re-run 'compounds init-hooks' to update

echo '{"additionalContext": "Write the per-project spec. After all per-project specs, call validate_project_specs(workflow_id=...) to check coverage."}'
exit 0
