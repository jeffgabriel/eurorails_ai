#!/bin/bash
# compounds-hooks v3 — installed by 'compounds init-hooks'
# Do not edit manually — re-run 'compounds init-hooks' to update

echo '{"additionalContext": "Write the spec using the returned template, then call validate_master_spec(workflow_id=...) to validate before proceeding."}'
exit 0
