#!/bin/bash
# compounds-hooks v3 — installed by 'compounds init-hooks'
# Do not edit manually — re-run 'compounds init-hooks' to update

echo '{"additionalContext": "Implement the task following the returned prompt. When done, call implement_task_finalize() to validate and complete."}'
exit 0
