#!/bin/bash
# compounds-hooks v5 — installed by 'compounds init-hooks'
# Do not edit manually — re-run 'compounds init-hooks' to update

echo '{"additionalContext": "The task prompt is in this response — implement directly from it. Do NOT call get_task — implement_task already returned everything you need. When done, call implement_task_finalize() to validate and complete."}'
exit 0
