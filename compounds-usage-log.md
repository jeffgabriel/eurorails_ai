# Compounds Skill Usage Log

Tracks when the compounds skill was used and what benefit it provided.

| Date | Task/Context | Benefit |
| 2026-03-09 | JIRA-73: Initial build budget underuse — why bot only spends 5M of 20M | Compounds search: "how build target is selected during initial build turns" surfaced PlanExecutor.executeInitialBuild, findInitialBuildTarget, computeBuildSegments architecture. Second search: "computeBuildSegments budget spending and when it stops" revealed tiebreaker logic (closest→cheapest→most segments at line 492-503) and ferry waypoint handling. Structural query: "computeBuildSegments -r calls" traced callers (ActionResolver.resolveBuild, tests). Compounds mapped the full flow: PlanExecutor picks ONE target → ActionResolver.resolveBuild calls computeBuildSegments ONCE → returns cheapest path → TurnComposer skips Phase B for initialBuild. Identified 4 compounding issues across 3 files. |
| 2026-03-09 | Ferry movement + A2 continuation research in TurnComposer | Compounds identified all key components (resolveFerryCrossing, getBotSpeed, findMoveTargets, computeReachableCities, splitMoveForOpportunities) and their relationships quickly; guided targeted file reads |
| 2026-03-08 | JIRA-62: A2 continuation intra-city truncation bug | Compounds search: "A2 continuation MOVE path truncation movement budget intra-city" surfaced RouteValidator, ContextBuilder, PlanExecutor, ActionResolver, TurnComposer architecture overview. Second search: "computeEffectivePathLength intra-city hop major city movement discount" confirmed `computeEffectivePathLength` + `isIntraCityEdge` in majorCityGroups.ts as the correct discount mechanism. Structural query: "truncatePathToEffectiveBudget" confirmed new helper not yet indexed. Compounds validated the architectural understanding and confirmed the fix approach uses the right shared utilities. |
| 2026-03-07 | JIRA-49 research: tryAppendBuild, findDemandBuildTarget, ContextBuilder affordability, impact analysis | Compounds provided full code snippets for tryAppendBuild (line 634), findDemandBuildTarget (line 391), isBuildAffordable (line 347), scoreDemand (line 1567). Impact analysis traced call chain: onTurnChange -> takeTurn -> compose -> tryAppendBuild -> findDemandBuildTarget/resolve. Semantic search surfaced isBuildAffordable and DemandContext.isAffordable field — key to identifying the missing affordability gate in Phase B build targeting. |
| 2026-03-08 | JIRA-56/57/58/59/60 research: 5-bug game log analysis | Compounds CLI: (1) compounds agent-prompt cli-usage (CLI reference), (2) 5 semantic searches with --show-code: "demandRanking AIStrategyEngine guardrail" (mapped GuardrailEnforcer.checkPlan at line 36 with G1/Stuck/G3/G8 priority chain, bestDelivery at line 151, renderDemandRanking at DebugOverlay.ts:638, scoreDemand at ContextBuilder.ts:1572, bestDemandForCard at ContextBuilder.ts:1139), "splitMoveForOpportunities A1 scanner pickup" (mapped splitMoveForOpportunities at TurnComposer.ts:506 with path-walking loop, delivery check at line 534, pickup check at line 553 with effectiveCapacity/reservedSlots/feasibility pre-filter, resolvePickup at ActionResolver, computeCanPickup at ContextBuilder), "build target selection secondary findMoveTargets" (mapped findMoveTargets at TurnComposer.ts:705 with 4-priority chain, findDemandBuildTarget at PlanExecutor.ts:391 sorting by min trackCost, findInitialBuildTarget at PlanExecutor.ts:410), "stuck detection guardrail progress counter reset" (mapped countNewSegments at computeBuildSegments.ts:556, advanceStop at PlanExecutor.ts, GuardrailResult type), "computeUpgradeAdvice upgrade recommendation" (mapped computeUpgradeAdvice at ContextBuilder.ts:1302 with ROI data, checkCanUpgrade at ContextBuilder.ts:1358, resolveUpgrade at ActionResolver). (3) compounds summary (5285 entities, 3454 in tests). (4) compounds impact takeTurn --direction both --max-depth 2 (39 affected: compose/checkPlan/executePlan/heuristicFallback/capture/getMemory/updateMemory at depth 1, onTurnChange/emitTurnChange at depth 2). (5) compounds impact checkPlan --direction both --max-depth 2 (8 affected: takeTurn/bestDelivery/primaryActionType at depth 1, onTurnChange at depth 2). Key findings: demandRanking computed once at end of takeTurn from context.demands (line 431) and sent to client — never refreshed mid-game; noProgressTurns counter at line 366 resets on hadDelivery/hadCashIncrease/hadNewTrack/isActivelyTraveling but "isActivelyTraveling" requires both loads AND activeRoute; computeUpgradeAdvice has ROI logic but only fires advice text without forcing action; findDemandBuildTarget sorts by min(trackCostToSupply, trackCostToDelivery) but doesn't consider demand score or route alignment.
| 2026-03-06 | Research: Bot starting city selection flow | Compounds CLI: `compounds index .` (reindex), then 5 semantic searches: "starting city selection bot", "ContextBuilder system prompt starting", "chooseStartingCity supply delivery", "planRoute starting city context", "LLMStrategyBrain starting city". Compounds returned generic/unrelated components (App.tsx, sidebar, lobby) — the AI pipeline files were not surfaced. Fell back to Grep which immediately found 50+ references across AIStrategyEngine, LLMStrategyBrain, ActionResolver, PlanExecutor, ResponseParser, schemas, systemPrompts, TurnComposer, and ContextBuilder. Compounds was not effective for this query — the AI service layer was not well-indexed or the search terms didn't match the vector embeddings. |
| 2026-03-06 | JIRA-38 bug investigation: Same-city multi-pickup chaining | Compounds CLI: (1) compounds index . (incremental reindex, 5241 entities), (2) compounds status (5241 entities/embeddings, consistent graph/vector), (3) 5 semantic searches with --show-code: "TurnComposer splitMoveForOpportunities A1 pickup same city" (mapped splitMoveForOpportunities at TurnComposer.ts:398 — walks path positions, checks DELIVER then PICKUP at each city, uses ActionResolver.resolve + applyPlanToState for simulation; also mapped resolvePickup at ActionResolver.ts:426, handlePickupLoad at TurnExecutor.ts:442, setLoadInCity at LoadService.ts:221), "TurnComposer compose findMoveTargets" (mapped full compose() pipeline at TurnComposer.ts:68 — Phase A operational enrichment with A0 deliver-before-build, A1 splitMoveForOpportunities, A2 continuation MOVE chaining loop with findMoveTargets priority chain, A3 prepend MOVE before BUILD; findMoveTargets at TurnComposer.ts:594 with 4 priority tiers: route stops, delivery cities, supply cities, reachable fallback; resolveMove at ActionResolver.ts:212 and PlanExecutor.ts:286), "PlanExecutor stop advancement current city" (mapped advanceStop at PlanExecutor.ts:473, skipCompletedStops at PlanExecutor.ts:501, isBotAtCity at PlanExecutor.ts:539, routeCompleted, getBotCityName), "LLMStrategyBrain route stop pickup" (mapped planRoute at LLMStrategyBrain.ts:239, decideAction at LLMStrategyBrain.ts:103, checkPickupFeasibility at RouteValidator.ts:135, StopValidation interface), "ActionResolver resolvePickup applyPlanToState" (mapped applyPlanToState at ActionResolver.ts:770 — switch on plan type: MoveTrain updates position, PickupLoad pushes to loads array, DeliverLoad splices load + adds payout + removes demand card, BuildTrack deducts cost + pushes segments; resolveDropLoad at ActionResolver.ts:500, resolveDeliver at ActionResolver.ts:383). (4) compounds impact splitMoveForOpportunities --direction both --max-depth 3 (9 affected: depth 1 = compose/getBotCapacity/resolve/warn/applyPlanToState/push/loadGridPoints, depth 2 = takeTurn, depth 3 = onTurnChange in BotTurnTrigger.ts:57). (5) compounds flows --entry-point compose --max-depth 5 (14 flows: compose→splitMoveForOpportunities→resolve→resolveSingleAction→resolveMove/resolveDiscard/resolveDeliver, compose→splitMoveForOpportunities→resolve→resolveMultiAction→warn/cloneSnapshot/resolveMove, compose→findMoveTargets→add/push, compose→planActionTypes). |
| 2026-03-06 | JIRA-34: Ferry-aware track cost estimation — full 2-task implementation via Compounds | Compounds plan_change routed to standard workflow → gen_spec produced tech spec with 3-branch estimation design → create_project + add_task created 2-task project. Task 1: extracted shared `computeLandmass()` and `computeFerryRouteInfo()` from inline BFS in computeBuildSegments.ts into MapTopology.ts, added 9 unit tests. Task 2: updated ContextBuilder.estimateTrackCost() with landmass detection and 3-branch ferry-aware estimation (same-landmass Dijkstra / cross-water ferry-paid / cross-water no-ferry-access), added 4 unit tests. All 206 tests pass (114 ContextBuilder, 45 computeBuildSegments, 47 MapTopology). Used update_task workaround for implement_task_finalize session state issue. |
| 2026-03-06 | JIRA-36: Animated Bot Train Movement — codebase exploration for change planning | Compounds CLI: (1) compounds index . (reindex), (2) compounds status (5227 entities, consistent graph/vector), (3) compounds summary (100% TypeScript, 5202 entities, 3241 server, 1715 client, 263 shared), (4) 6 semantic searches with --show-code: "TurnExecutor ExecutionResult movementPath" (mapped ExecutionResult interface at TurnExecutor.ts:24 — has success/action/cost/segmentsBuilt/remainingMoney/durationMs/error/payment/newCardId but NO movementPath field yet; also mapped client MovementExecutor.executeMovement flow and PlanExecutor.resolveMove), "BotTurnTrigger bot:turn-complete socket emit" (mapped full onTurnChange at BotTurnTrigger.ts:57 with emitToGame bot:turn-complete payload — movementData currently only has from/to/mileposts/trackUsageFee, no path array), "AIStrategyEngine BotTurnResult" (mapped BotTurnResult interface at AIStrategyEngine.ts:43 — has movedTo but no movementPath; mapped full takeTurn pipeline including milepostsMoved extraction from composed plan), "GameScene onPatch train position" (mapped UIManager.updateTrainPosition → TrainInteractionManager.updateTrainPosition → TrainSpriteManager.createOrUpdateSprite chain), "TrainSpriteManager sprite createOrUpdate" (mapped full TrainSpriteManager class — createOrUpdateSprite uses setPosition for instant teleport, getSprite returns Phaser.GameObjects.Image, scene.add.image creates sprites), "bot:turn-complete socket event" (confirmed client socket.ts onTurnChange listener pattern and full BotTurnTrigger emission). (5) compounds impact executePlan --direction both --max-depth 3 (7 affected entities: direct callers takeTurn/planToOption/executeMultiAction/execute, depth 2 onTurnChange, depth 3 emitTurnChange/onHumanReconnect). (6) compounds flows --entry-point advanceTurnAfterBot --max-depth 5 (18 flows traced including advanceTurnAfterBot→advanceTurn→emitTurnChange→onTurnChange→takeTurn chain). (7) compounds flows --entry-point takeTurn --max-depth 5 (25 flows including compose→splitMoveForOpportunities, planRoute→parseStrategicRoute, build→computeTrackSummary). |
| 2026-03-05 | JIRA-32: NDJSON game log for fast bot turn debugging | Compounds CLI: `compounds search "bot turn audit logging llmLog turn composition"` mapped DecisionLogger (initTurnLog/logPhase/flushTurnLog pipeline), LLMPhaseFields interface, QueuedTurn in BotTurnTrigger. Identified that DecisionLogger outputs to console only (not persisted), and TurnComposer internals are only console.log. Compounds DB was down initially — restarted and re-queried successfully. Used compounds to confirm DemandContext fields (efficiencyPerTurn, estimatedTurns, trackCost) already computed but not emitted, informing the enriched demand ranking design. |
| 2026-03-04 | Bot movement composition bug (stale isLoadOnTrain flag) | Compounds CLI: compounds search "bot movement delivery turn composition" --show-code → mapped TurnComposer.compose() pipeline, A1 splitMoveForOpportunities, A2 continuation loop, findMoveTargets priority chain. compounds search "TurnExecutor movement execution after delivery" → mapped TurnExecutor.executeMultiAction sequential step processing. compounds search "ActionResolver applyPlanToState delivery simulation" → found that applyPlanToState updates context.loads but NOT context.demands flags (root cause). Key finding: findMoveTargets() used stale demand.isLoadOnTrain instead of live context.loads, causing post-delivery movement to fail. Fix: replaced stale flags with context.loads.includes() checks + added Priority 4 reachableCities fallback. |
| 2026-03-03 | JIRA-24: Tech spec codebase research (8-bug bot stuck pipeline) | Full Compounds CLI suite: (1) compounds status (4337 entities, consistent graph/vector), (2) compounds summary (99% TypeScript, 2424 server entities, 1673 client, 232 shared), (3) compounds dependencies --scope external (77 packages: neverthrow, pg, phaser, express, react), (4) compounds dependencies --scope internal (158 modules — mapped key AI pipeline: AIStrategyEngine 6 imports, ActionResolver 7 imports, TurnComposer, PlanExecutor 3 each, GuardrailEnforcer 2, BotMemory 5). (5-16) 12 semantic searches: "opportunistic pickup load" (mapped TurnComposer.splitMoveForOpportunities pickup path with NO delivery feasibility check — root cause of Bug 1), "splitMoveForOpportunities" (found 3 call sites in TurnComposer, no capacity reservation — Bug 2), "TurnComposer enrich" (mapped compose() pipeline: Phase A operational + Phase B build), "cargo capacity train full" (mapped TrainProperties.capacity, getBotCapacity, resolvePickup capacity gate), "drop load" (mapped TurnExecutor.handleDropLoad + loadService.returnLoad — exists but never auto-triggered by pipeline — Bug 3), "abandoned route replan" (mapped planRoute → PlanExecutor.execute → routeAbandoned flow, confirmed lastAbandonedRouteKey written but NEVER READ — Bug 4), "bot turn audit" (mapped BotTurnTrigger audit INSERT, StrategyAudit interface), "GuardrailEnforcer override" (found Guardrail 4 empty-path override — Bug 6), "heuristicFallback" (mapped deliver>pickup>move>discard>build priority chain), "AIStrategyEngine pipeline" (mapped full 5-stage orchestrator with TurnComposer compose step), "PlanExecutor route execution" (mapped 2-question model + skipCompletedStops), "ActionResolver demand scoring" (mapped resolvePickup FR-6 feasibility check that splitMoveForOpportunities BYPASSES). Direct file reads: AIStrategyEngine.ts (full 567 lines), TurnComposer.ts (full 555 lines), GuardrailEnforcer.ts (full 355 lines), PlanExecutor.ts (full 468 lines), ActionResolver.ts resolvePickup/heuristicFallback, ContextBuilder.ts computeCanPickup, BotMemory.ts (confirmed lastAbandonedRouteKey not in defaultState), GameTypes.ts BotMemoryState. |
| 2026-03-03 | JIRA-23: FE-001 client-side city_internal cost fix | Compounds implement_task_finalize workflow: get_task retrieved full subtask IDs (4 subtasks), complete_subtasks marked all done, implement_task_finalize(validate) provided full validation checklist, implement_task_finalize(mark_done) confirmed all_tasks_complete (5/5 tasks done). Auto-proceed mode enabled seamless commit+push+finalize flow. |
| 2026-03-03 | JIRA-21: Implement TurnComposer A2 loop for continuous movement chaining (BE-001) | Compounds implement_all_tasks orchestrated single-task project: implement_task provided full task prompt with 8 acceptance criteria and 6 subtasks. Loaded 6 design patterns (gaming-llm-action-interface, gaming-game-loop, gaming-game-state-perception, anti-patterns-error-swallowing, testing-test-pyramid, testing-mocking-boundaries) via get_design_patterns. implement_task_finalize enforced validation gate (phase=validate → phase=mark_done with evidence). All 45 tests pass (38 existing + 7 new). Committed and pushed to compounds/204v5. |
| 2026-03-03 | JIRA-20: Tech spec generation for movement budget bug fix | Full Compounds workflow: (1) get_all_projects to find project, (2) get_project_status to check PRD/spec state, (3) spec_project for orchestration, (4) tech_spec_research for research prompt, (5) Compounds CLI: compounds status (4337 entities, consistent), compounds summary (architecture overview), 4 semantic searches — "resolveMultiAction cumulative movement speed budget tracking" (mapped ActionResolver multi-action loop gap), "TurnComposer countMovementUsed remaining movement speed" (found existing countMovementUsed pattern at line 465), "GuardrailEnforcer validate movement plan enforcer" (confirmed zero movement checks in checkPlan), "TurnExecutor execute movement steps MultiAction" (mapped execution pipeline). (6) compounds dependencies --scope internal (mapped 158 internal modules, ActionResolver has 7 importers), (7) get_pattern_context + pattern_detection (selected 5 patterns: gaming-game-loop, gaming-llm-action-interface, anti-patterns-error-swallowing, anti-patterns-god-service, common-code-readability), (8) get_design_patterns (loaded full content for all 5), (9) get_testing_frameworks (unit + integration for typescript-react), (10) generate_tech_spec (wrote spec following bugfix scenario template), (11) compounds upload (tech spec uploaded), (12) plan_project (task breakdown initiated). |
| 2026-03-02 | JIRA-19: DebugOverlay investigation (3 Compounds CLI searches + file reads) | Searched: (1) "debug overlay bot turn complete event handling and display" → mapped full data flow from BotTurnTrigger.onTurnChange() socket emission through client socket.ts to DebugOverlay.logSocketEvent(). Identified BotTurnResult fields emitted vs dropped. (2) "DebugOverlay component strategy inspector modal bot decision display" → found StrategyInspectorModal.tsx (React/Dialog) with LLMDecisionSection showing model/latency/tokens, and DebugOverlay.ts (Phaser/HTML) showing only reasoning/planHorizon. (3) "bot:turn-complete socket event emission fields" → confirmed exact field list in BotTurnTrigger lines 126-147, identified model/llmLatencyMs/tokenUsage/retried are NOT emitted. Key findings for JIRA-19 scope expansion: DebugOverlay uses single `lastBotTurnInfo` slot (multi-bot overwrite bug), no turn history, missing LLM metadata fields. |
| 2026-03-02 | JIRA-18: Full 9-task implementation via Compounds implement_all_tasks | Compounds orchestrated all 9 tasks: BE-000 (model defaults), BE-001 (isGemini3Model), BE-002 (thinkingConfig), BE-003 (skip structured output), BE-004 (thought filtering), BE-005 (provider-agnostic gating), BE-006 (schema-rejection retry), TEST-001 (33 GoogleAdapter tests), TEST-002 (37 LLMStrategyBrain tests incl. 6 new Google provider integration tests). implement_task provided per-task prompts with acceptance criteria. get_project_tasks/get_project_status tracked completion. All 9 tasks DONE, 70 tests passing across both test suites. |
| 2026-03-02 | JIRA-18: Gemini 3 Thinking Support + Model Upgrade codebase research (10 Compounds CLI commands) | Ran compounds agent-prompt cli-usage, status (4337 entities, consistent), summary, dependencies (external 77 packages + internal 158 modules), and 5 semantic searches: (1) "GoogleAdapter" → full GoogleAdapter.ts source (chat method using v1beta REST API, system_instruction/contents/generationConfig body, no thinking support), ProviderAdapter interface (ThinkingConfig type), createAdapter factory with use46 gating, (2) "thinking config provider adapter" → mapped adapter pattern: ProviderAdapter base, GoogleAdapter/AnthropicAdapter impls, createAdapter in LLMStrategyBrain, ThinkingConfig interface (type: string), AnthropicAdapter thinking+effort+outputSchema handling, (3) "LLMStrategyBrain provider gating" → mapped full gating: use46 = provider===Anthropic && supports46Features(), supports46Features checks model.includes('opus-4-6'/'sonnet-4-6'), ACTION_EFFORT/ROUTE_EFFORT maps by BotSkillLevel, LLM_DEFAULT_MODELS lookup, (4) "Gemini API response parsing" → ResponseParser with parse/parsePlanSelection/parseActionIntent/parseStrategicRoute methods, markdown fence stripping, regex fallback, structured output fast-path, (5) "effort thinking level" → BotSkillLevel enum (Easy/Medium/Hard), ACTION_EFFORT (low/medium/high), ROUTE_EFFORT (medium/high/high), TEMPERATURE_BY_SKILL mappings. Key findings for JIRA-18: GoogleAdapter has NO thinking/effort/outputSchema support, use46 gating is Anthropic-only, ThinkingConfig is documented as "Anthropic's adaptive thinking", provider gating needs expansion for Gemini thinking models. |
| 2026-03-02 | JIRA-14 continued: Tasks 7-9 (BE-006, FE-001, TEST-001) | Compounds get_task/get_task_with_context provided full task prompts with acceptance criteria for each task. implement_task + implement_task_finalize tracked completion. get_project_tasks for status overview. FE-001 task prompt guided TurnExecutor delivery handler modification and DebugOverlay socket listener. TEST-001 prompt defined monitoring framework requirements. All 9 tasks completed, 494 tests passing across 15 AI test suites. |
| 2026-03-02 | JIRA-14: Codebase research via 16 Compounds CLI queries | Ran compounds summary, dependencies (external + internal), and 13 semantic searches covering: PlanExecutor stop index advancement, StrategyInspectorModal demand ranking, turn_actions pickup recording, routeComplete continuation move, AIStrategyEngine retry/fallback/heuristic, computeBuildSegments ferry port waypoint, resolveMove truncated path movement, ferry teleportation half speed, getFerryEdges ferry pairs, ferryHalfSpeed WorldSnapshotService, AI pipeline test mocking, TurnComposer pickup chaining, TurnExecutor action recording. Mapped full AI pipeline architecture, ferry data model (FerryEdge/FerryConnection/FerryPoint types, getFerryEdges from majorCityGroups.ts, ferryHalfSpeed flag in WorldSnapshotService), PlanExecutor stop advancement (currentStopIndex, advanceStop, advancePhase), ActionResolver.resolveMove ferry truncation logic, heuristicFallback fallback chain, TurnExecutor DB patterns (BEGIN/COMMIT + best-effort audit). |
| 2026-03-02 | JIRA-16: Strategic Intelligence — full 8-task implementation via Compounds | Compounds implement_all_tasks orchestrated 8 tasks across 4 commits: BE-001 (system prompt overhaul), BE-002/003/004/TEST-002 (secondaryBuildTarget pipeline removal), BE-005 (hand quality + best-demand-per-card), BE-006 (enhanced demand ranking), TEST-001 (ContextBuilder test updates). implement_task provided per-task prompts with acceptance criteria and subtask tracking. implement_task_finalize enforced validation gates. Fixed 6 pre-existing ContextBuilder test failures. 196 tests passing across 5 AI pipeline suites. |
| 2026-02-28 | JIRA-13: Demand scoring fix — replace UNAFFORDABLE gate with network-value ranking | Compounds medium_planner workflow orchestrated full implementation: change_planner → medium_planner → CLI discovery (compounds search for demand scoring, reachability, debug overlay, network value) → pattern_detection → spec generation → upload → plan_project (10 tasks auto-generated) → implement_all_tasks (dependency-ordered). Implemented scoreDemand() + computeCorridorValue() in ContextBuilder, removed UNAFFORDABLE gate, added DEMAND RANKING to LLM prompt, wired demandRanking through AIStrategyEngine → BotTurnTrigger → DebugOverlay. 5 new tests, 455/463 pass (8 pre-existing). |
| 2026-02-27 | Turn Composition Flow: full 23-task parallel implementation via Compounds workflow | Compounds orchestrated complete project lifecycle: spec_project → tech_spec_research → pattern_detection (8 patterns selected from gaming-llm-game-agent ref arch) → generate_tech_spec → validate_spec (all checks pass) → plan_project (23 tasks generated) → implement_all_tasks (priority ordering). Team of 4 agents implemented in parallel: turn-composer (BE-001/005/008/010), strategy-engine (BE-002/004/006/011), resolver-guardrail (BE-003/007), plan-executor (BE-009). 208 tests passing (1 pre-existing failure), 6 test suites. Key changes: PlanExecutor 2-question model refactor, heuristic fallback removal, 230M victory build threshold, no-pass-with-loads guardrail, delivery-clears-route for re-planning. |
| 2026-02-26 | Turn composition flow doc rewrite — verify pipeline order and code behavior | Compounds ran 3 searches: (1) "AIStrategyEngine executeTurn pipeline order" → confirmed takeTurn calls snapshot→context→decision gate→TurnComposer.compose→guardrails→execute, (2) "PlanExecutor execute route stop phase" → confirmed phase order is build→travel→act with travel→build fallback when move fails, (3) "auto-place train position major city starting city" → confirmed auto-placement is in AIStrategyEngine.takeTurn gated by `gameStatus !== 'initialBuild'` (line 86), happens at start of normal play not during initial build. Used to correct doc errors about pipeline ordering and auto-placement timing. |
| 2026-02-26 | JIRA-8 ferry build gap + demand hallucination investigation | Compounds ran 6 searches: (1) "computeBuildSegments segment extraction budget path nodes" → mapped extractSegments run-based logic and connectedFromPath validation, (2) "resolveMove pathfinding validate track connectivity movement" → mapped resolveMove → computeTrackUsageForMove → buildUnionTrackGraph pipeline, (3) "route planning prompt demand cards LLM serialize loads carrying" → mapped serializeRoutePlanningPrompt demand card format and planRoute retry mechanism, (4) "Ruhr major city group outposts center milepost positions" → found MajorCityGroup type and getMajorCityGroups derivation from gridPoints.json, (5) deep agent investigation of extractSegments (traced connectedViaBuilt only-built-edges bug causing post-ferry run filtering), (6) deep agent investigation of demand format ambiguity ("Imports from London → Budapest" confuses supply/delivery cities). Root causes: extractSegments:606-613 doesn't propagate connectivity through new segments, demand format at ContextBuilder:767 uses ambiguous "from X → Y" instead of explicit PICKUP/DELIVER labels. |
| 2026-02-26 | JIRA-7 turn workflow gaps analysis | Compounds searches from prior session ("TurnComposer compose findMoveTarget continuation move after deliver route sync" and "heuristicFallback discard hand option single action no composition") confirmed: (1) findMoveTarget has no deliver-stop skip logic, (2) heuristicFallback returns single actions without composition, (3) route currentStopIndex is never advanced during composition, (4) applyPlanToState delivery payout modeling needs verification. These findings informed 5 gaps in the JIRA-7 ticket. |
| 2026-02-25 | Speculative victory build mid-route investigation (Ruhr build bug) | Compounds search "tryAppendBuild victory progress unconnected major cities build target selection" confirmed architecture: TurnComposer.tryAppendBuild → falls through to victory fallback when route stops are on network → picks cheapest unconnected major city (Ruhr). Root cause: no guard for mid-route (travel/act phase) — bot spent 10M/14M on speculative Ruhr build while actively delivering Cars to Nantes. Fix: skip speculative victory/demand builds when activeRoute.phase is 'travel' or 'act'. |
| 2026-02-25 | TurnComposer continuation MOVE after pickup/deliver at destination | Compounds search "applyPlanToState MoveTrain update position speed movement used" confirmed applyPlanToState does NOT track movement used (only updates position and deducts fees). Compounds search "resolveMove path truncate speed milepost cap movement" confirmed resolveMove caps paths at full train speed without considering prior movement. Together these confirmed the three bugs: (1) A2 condition skipped when hasMove=true from A1 split, (2) findMoveTarget returned the just-completed pickup stop, (3) no movement cap for continuation moves. |
| 2026-02-25 | TurnComposer splitMoveForOpportunities city lookup bug | Compounds search "city name lookup from grid position row col for all cities including medium small" found getCityNameAtPosition in ContextBuilder.ts and confirmed loadGridPoints() in MapTopology.ts returns GridPointData with name for ALL city types (major, medium, small). Compounds search "loadGridPoints grid point name city milepost" showed loadGridPoints returns Map<string, GridPointData> keyed by "row,col" with name field for all named mileposts. This confirmed the fix: replace getMajorCityLookup() (major cities only) with loadGridPoints() (all cities) in TurnComposer.splitMoveForOpportunities, enabling mid-movement pickup/deliver at medium and small cities. |
| 2026-02-25 | Porto bankruptcy bug: PlanExecutor speculative build investigation | Compounds search "PlanExecutor building toward demand city when all route stops reachable" identified findDemandBuildTarget() and findInitialBuildTarget() methods, executeBuildPhase flow, and TurnComposer speculative build integration. Traced full call chain: executeBuildPhase → findInitialBuildTarget (returns null when all stops reachable) → findDemandBuildTarget (sorted by highest payout → picked Porto). Also identified TurnComposer line 292 calling same method for post-action speculative builds. Root cause: highest-payout sort in findDemandBuildTarget picked distant expensive city. |
| 2026-02-25 | Gemini provider + model selection investigation | Compounds ran 5 searches: (1) "LLM provider configuration API key model selection" → mapped full adapter pattern (ProviderAdapter interface, AnthropicAdapter, GoogleAdapter, LLMProvider enum, LLM_DEFAULT_MODELS), (2) "LLMStrategyBrain how it calls the AI model API" → traced pipeline from createAdapter→chat→response parsing, (3) "bot difficulty level model selection easy medium hard" → mapped BotSkillLevel enum, getBotSpeed, getBotCapacity, heuristicFallback per skill, (4) structural query "LLMStrategyBrain" imports → 18 dependency relationships, (5) "LLM adapter provider pattern AnthropicAdapter" → confirmed GoogleAdapter already exists and is wired. Found: Google Gemini is ~95% implemented. Only gap is lobby route + client API don't transmit provider/model fields. |
| 2026-02-25 | JIRA-5 Route Planning Context Enrichment: full project execution (11 tasks, 56 subtasks) | Compounds orchestrated complete workflow: spec_project → tech_spec_research → pattern_detection → generate_tech_spec → validate_spec → plan_project (11 tasks generated) → implement_task → complete_subtasks (56 subtasks) → implement_task_finalize (validate + mark_done for all 11). Implementation: 7 BE tasks (DemandContext enrichment, computeCorridors, detectOnTheWay, serializeRoutePlanningPrompt, formatReachabilityNote scarcity, system prompt criteria, planRoute integration) + 4 TEST tasks (60 new tests). All 165 AI tests passing. Project marked all_tasks_complete. |
| 2026-02-25 | JIRA-5 Route Planning Context Enrichment: codebase research (12 compounds commands) | Ran compounds agent-prompt cli-usage, status, summary, dependencies (external + internal), and 7 semantic searches mapping: (1) ContextBuilder.serializePrompt demand serialization flow (formatReachabilityNote, formatVictoryBonus, computeCanDeliver), (2) hexDistance in MapTopology.ts (cube-coordinate hex distance), (3) WorldSnapshot.loadAvailability (Record<string, string[]> binary availability, no chip counts), (4) ROUTE_PLANNING_SYSTEM_SUFFIX prompt (route planning criteria, initial build strategy), (5) DemandContext estimated costs (estimateTrackCost, computeDemandContext, findBestSupplyCity), (6) serializePrompt plan persistence section (previousTurnSummary + PLAN PERSISTENCE warning), (7) getMajorCityGroups (center + outposts positions for corridor computation). Compounds mapped full data pipeline for enrichment: what data exists (hexDistance, MajorCityGroup positions, DemandContext costs, binary load availability) vs what is missing (corridor analysis, on-the-way signals, turn estimates, chip scarcity counts). |
| 2026-02-24 | Bug fix: bot only built 1 segment + chose bad initial route | Compounds search "PlanExecutor executeBuildPhase phase transition travel initialBuild" identified the executeBuildPhase method, its phase transition logic, and the executeTravelPhase initialBuild guard. Compounds search "ContextBuilder initial build cost estimate demand serialization" mapped the full cost estimation pipeline: computeDemandContext → estimateTrackCost (cold-start uses nearest major city, not supply city), findBestSupplyCity, isFerryOnRoute, formatReachabilityNote → affordabilityTag. Two root causes found and fixed: (1) PlanExecutor built toward starting city instead of delivery during initialBuild, (2) cost estimates used nearest-major-city-to-delivery instead of supply-to-delivery distance, making Wien→Birmingham look like 6M instead of 35M+. Also fixed ferry detection for cross-Channel routes. |
| 2026-02-24 | Starting City Fix: full 5-task implementation via compounds workflow | Compounds orchestrated the complete project lifecycle: get_project (context), implement_task (5 tasks: BE-004, BE-002, BE-001, BE-003, TEST-001), complete_subtasks (all subtasks per task), implement_task_finalize (validate + mark_done for each). All 5 tasks implemented and finalized, 321 tests passing across 10 AI test suites. Project marked all_tasks_complete. |
| 2026-02-24 | Improve Bot Route Finding: full 5-task implementation via compounds workflow | Compounds orchestrated complete project lifecycle: get_project, get_task_with_context (5 tasks: BE-001 through BE-004 + TEST-001), complete_subtasks (25 subtasks), implement_task_finalize (validate + mark_done for each). Three fixes: (1) hexDistance extracted from ContextBuilder to MapTopology, (2) findCityMilepost excludes major city centers for BUILD via forBuild param, (3) resolveBuild sorts targets by proximity to frontier, (4) computeBuildSegments uses hexDistance instead of Euclidean. All 363 tests passing across 12 suites. Project marked all_tasks_complete. |
| 2026-02-24 | Architecture discovery for Starting City Fix: 11 compounds commands (summary, dependencies --scope external, dependencies --scope internal, 8 semantic searches) | Compounds summary identified 4337 entities across src/server (2424), src/client (1673), src/shared (232). External deps (77 packages) mapped React, pg, Phaser, neverthrow, zustand, zod stack. Internal deps (158 modules) ranked GameTypes (113 imports), db (50), MapTopology (10), ActionResolver (7), AIStrategyEngine (6) as highest-coupled AI modules. Index was on main branch so semantic searches for branch-specific AI code (AIStrategyEngine, ActionResolver, computeBuildSegments, PlanExecutor, StrategicRoute) returned generic results rather than the modified pipeline code. Fell back to direct file reads for the 8 AI pipeline files on the working branch. Compounds was valuable for the structural overview (commands 1-3) but less effective for feature searches (commands 4-11) due to main-branch indexing. |
| 2026-02-24 | Starting City Fix gap analysis: 10 compounds searches for PRD requirements | Compounds index had 0 embeddings (2997 entities, 0 embeddings). All 10 searches returned empty. Triggered `compounds index` in background. Fell back to grep/glob + direct file reads for the full gap analysis. |
| 2026-02-24 | Remove Archetypes Epic: codebase research | Ran 10 compounds searches (BotArchetype enum definition, archetype prompt system, archetype UI component, getSystemPrompt archetype, bot config archetype, LLMStrategyBrain archetype, lobby archetype bot creation) plus summary and dependencies. Compounds identified all 25 files referencing archetype, mapped the full flow from enum definition through BotConfig, LLMStrategyBrain constructor, systemPrompts.ts ARCHETYPE_PROMPTS mapping, lobby creation, UI display (BotConfigPopover, ArchetypeBadge, StrategyInspectorModal, botDisplayUtils), and 10 test files. Semantic search was especially valuable for finding the ArchetypeBadge component and StrategyInspectorModal ARCHETYPE_PHILOSOPHY mapping that simple grep would have required multiple patterns to discover. |
| 2026-02-20 | Document AI LLM decision-making pipeline narrative | Compounds identified all 7 pipeline components (BotTurnTrigger, AIStrategyEngine, ContextBuilder, LLMStrategyBrain, ResponseParser, ActionResolver, GuardrailEnforcer, TurnExecutor) with call relationships, entry points, and retry logic. Faster than tracing imports manually. |
| 2026-02-20 | Plan plan-then-execute architecture change | Compounds semantic search found existing DeliveryPlan type, BotMemoryState.activePlan (unused), and confirmed no PlanExecutor exists yet. Structural query on DeliveryPlan showed only test usage. Saved time vs. manual grep across 15+ files. |
| 2026-02-20 | Bot behavior fixes: extractSegments ferry bug, buildTargetCity debug overlay, LLM prompt enrichment, cash reserve guardrail | 6 compounds searches: (1) `search "extractSegments ferry crossing handling"` mapped extractSegments → getFerryEdges → ferryConnections pipeline, (2) `search "buildTargetCity populated in bot turn result"` traced BotTurnResult → identifyTargetCity → AIStrategyEngine flow and confirmed field is never set, (3) `search "multi-action turn MOVE PICKUP DELIVER chaining"` revealed TurnExecutor.executeMultiAction pipeline, (4) `search "LLM strategy brain prompt system message"` mapped LLMStrategyBrain → getSystemPrompt → COMMON_SYSTEM_SUFFIX, (5) `search "getBuildBudget money reserve calculation"` identified budget doesn't subtract 5M reserve, (6) `search "GuardrailEnforcer checkPlan cash reserve"` confirmed no BUILD spending guardrail exists. |
| 2026-02-19 | ai-v6.3 full project execution: create project, upload PRD, generate tech spec, plan_project breakdown, implement all 35 tasks via 2-worker team | Compounds orchestrated the entire workflow: spec_project (research + pattern detection + spec generation + validation), plan_project (auto-generated 35 tasks from tech spec), implement_task (delivered task prompts with full context for each). Team of 2 workers completed all 35 tasks: 24 BE, 8 TEST, 1 FE, 2 INF. New pipeline: ContextBuilder + LLM + ActionResolver replaces OptionGenerator + Scorer. 258+ tests passing. |
| 2026-02-19 | prd-v6.3 gap analysis: verified TrackNetworkService, TrackBuildingService, LoadService, PlayerService, trackUsageFees, DemandDeckService, InitialBuildService APIs against spec | Found 7 gaps (see below). Compounds identified exact method signatures, parameter types, and missing methods — would have taken 10+ file reads with grep. |
|------|-------------|---------|
| 2026-02-18 | Extend plan system to initialBuild phase | 5 compounds queries validated the fix: (1) `search "plan system initialBuild game phase selectNewPlan PlanExecutor"` confirmed plan creation is gated to active-only, (2) `query "AIStrategyEngine" -r calls` traced call graph, (3) `search "executeLoadActions Phase 0 initialBuild gameStatus guard"` confirmed Phase 0 is already gated separately, (4) `search "PlanExecutor detectPhaseTransition build_to_pickup no position"` confirmed position checks only in travel/deliver phases (safe for no-position initialBuild), (5) `search "InitialBuildService advanceTurn bot turn trigger"` confirmed BotTurnTrigger→AIStrategyEngine→InitialBuildService flow is identical for both phases. Validated that extending two gameStatus guards is safe with no side effects. |
| 2026-02-18 | Ferry port targeting + crossing heuristic removal | 5 compounds queries: (1) `search "ferry port cost handling in Dijkstra pathfinding"` mapped computeBuildSegments → ferryPortCosts → getTerrainCost pipeline, (2) `search "ferry crossing decision logic"` traced handleFerryCrossing → Euclidean distance heuristic → all demand cities, (3) `query "computeBuildSegments" -r calls` found 2 callers (OptionGenerator + test), (4) `search "major city groups and ferry port mileposts"` revealed MajorCityGroup/FerryEdge relationship, (5) `query "getFerryEdges" -r calls` mapped 13 callers across build/move/validate paths. Compounds confirmed low blast radius for both fixes and identified the exact data flow causing ferry port inclusion in build targets. |
| 2026-02-18 | Post-v6.1 gameplay bug triage — 3 critical bugs | 3 compounds searches mapped: (1) pickup threshold gate in AIStrategyEngine (score < 15 blocks demand-matching pickups when delivery city unreachable), (2) build option index resolution path (LLMStrategyBrain.selectOptions → feasibleBuilds[index] uses original order not serializer's sorted order), (3) chainScore floor 0.01 in rankDemandChains (negative-profit chains all equal). Fixed all 3: removed alphabetical sort (index mismatch), lowered pickup threshold to 1 (was 15), replaced 0.01 floor with payment/turns*0.1 for meaningful differentiation. |
| 2026-02-18 | ai-v6.1 full implementation — 13 tasks via Compounds workflow | Used `create_project`, `upload` PRD, `plan_project` (13 tasks generated), then `get_project_tasks`/`update_task` to track all 13 tasks (BE-001 through BE-012 + DOC-001) through implementation. Wave 1 (4 tasks) done by 2 parallel agents, Waves 2-3 (9 tasks) done sequentially. All 13/13 marked DONE via compounds. Changes: LLM enabled during initialBuild, geography section, chain analysis section, shared track computation, budget feasibility, reuse annotations, top-5 chains, neutral ordering, Scorer documented as fallback-only. |
| 2026-02-18 | Hub selection favors peripheral cities (Madrid/Bilbao) over central Europe | 3 compounds searches mapped the full initial placement pipeline: evaluateHubScore → rankDemandChains → determineStartPositions → autoPlaceBot → generateBuildTrackOptions → calculateBuildTrackScore. Compounds revealed evaluateHubScore returns only the BEST single chain score, causing peripheral hubs with one good chain to beat central hubs with many viable chains. Fixed by summing top-3 chain scores, naturally rewarding central European hubs (Ruhr, Berlin, Paris) that enable multiple delivery chains. |
| 2026-02-18 | PRD v6.1 — investigate heuristic vs LLM decision boundaries | 6 compounds searches mapped: (1) LLM prompt construction pipeline (GameStateSerializer → ProviderAdapter → ResponseParser → GuardrailEnforcer), (2) response parsing and index validation flow, (3) heuristic override/guardrail/fallback chain. Read all 4 key files (GameStateSerializer, LLMStrategyBrain, ResponseParser, systemPrompts) to catalog exactly what context the LLM receives vs what's decided by heuristics. Key finding: LLM is completely skipped during initialBuild (gated behind `gameStatus === 'active'`), and prompt lacks geographic context, chain analysis, and reuse indicators. Informed 9-task PRD for enriching LLM context. |
| 2026-02-17 | ai-v6 project creation — PRD upload, tech spec generation, pattern detection, task breakdown | Full Compounds workflow: `create_project`, `upload` PRD, `spec_project`, `tech_spec_research`, `pattern_detection` (9 patterns selected from manifest), `get_design_patterns` (loaded full content), `generate_tech_spec`, `validate_spec`, `upload` tech spec, `plan_project` (generated 9 tasks with enriched prompts). Two research subagents ran 16 compounds search/query commands to map existing pipeline architecture, discover patterns, and confirm gaps. Breakdown agent automatically decomposed tech spec into ordered implementation tasks. |
| 2026-02-17 | ai-v6 full implementation — 13 tasks via Compounds workflow | Used `implement_task` to retrieve next task + prompts, `implement_task_finalize(validate)` → `implement_task_finalize(mark_done)` for all 13 tasks: INF-001, BE-001 through BE-011, TEST-001. `get_project` verified 100% completion. Team of 3 agents executed Wave 2 tasks in parallel. All test files verified passing (45 new tests + 85 existing). |
| 2026-02-16 | pickupTargets too broad — bot builds toward wrong city (Kaliningrad instead of Birmingham) | Compounds search "OptionGenerator BuildTrack option generation computeBuildSegments targets" mapped the full option generation flow: generateBuildTrackOptions → rankDemandChains → computeBuildSegments. Also "autoPlaceBot placement logic nearest major city" revealed the placement algorithm. Identified that pickupTargets included ALL cities with a load type, causing pathfinding to pick the easiest target (near Kaliningrad) instead of the strategically correct one (Birmingham near delivery Antwerpen). |
| 2026-02-16 | Terrain-aware cost estimation — rankDemandChains AVG_COST_PER_SEGMENT=1.5 underestimates Alpine/ferry routes | Compounds search "terrain cost per milepost building cost calculation" mapped the full cost calculation architecture: TrackBuildingService.calculateNewSegmentCost (authoritative), MapTopology.getTerrainCost (1/2/5/3/5 costs), computeBuildSegments.getWaterCrossingCost (+2/3M rivers/lakes), and ferryPortCosts (4-16M). Confirmed rankDemandChains uses flat 1.5M average that misses all terrain variation. |
| 2026-02-14 | Track usage fee bug — BFS picks shortest path through opponent track instead of own track | Identified all key files (trackUsageFees.ts, MovementExecutor.ts, UIManager.ts, PlayerStateService.ts) and the full data flow (computeTrackUsageForMove → bfsPath → confirmOpponentTrackFee popup) in a single query, enabling quick root cause identification |
| 2026-02-14 | Bot builds track randomly, never reaches demand cities | Compounds identified OptionGenerator, computeBuildSegments, identifyTargetCity, and determineStartPositions as the key components. Revealed that computeBuildSegments had no target parameter and generateBuildTrackOptions never passed demand info. Confirmed BuildTowardMajorCity action type existed but was never generated. |
| 2026-02-14 | Investigate server-side reversal enforcement | Compounds traced the full movement flow: moveTrainForUser → position update → movement_history. Found the client-side reversal logic (isReversalByDirectionFallback, isTerrainCityOrFerry) in TrainMovementManager.ts and confirmed no equivalent exists server-side. Also identified that movement_history already stores per-move segments server-side, providing the data needed for direction detection. |
| 2026-02-14 | Bot carries 3 loads on Freight train (capacity 2) | Two compounds queries mapped the full load pickup/delivery architecture: identified TurnExecutor.handlePickupLoad has no capacity check, and AIStrategyEngine pickup loop generates options once then iterates without re-checking capacity. Also confirmed deliverLoadForUser correctly removes loads. Pinpointed both root causes without manual file searching. |
| 2026-02-15 | Strategy Inspector tech spec research — 13 compounds queries | Mapped entire AI bot pipeline (WorldSnapshotService, OptionGenerator, Scorer, PlanValidator, TurnExecutor, AIStrategyEngine, BotTurnTrigger), existing debug infrastructure (DebugOverlay with backtick toggle), socket event patterns (bot:turn-start, bot:turn-complete, turn:change), UI modal patterns (Phaser rexUI modals, React Radix dialogs), audit DB table schema (bot_turn_audits), and confirmed StrategyInspectorModal does NOT exist in source. All 13 queries returned relevant components with source code. |
| 2026-02-14 | Fix bot oscillation (München↔Milan) — Scorer needs pickup awareness | Compounds identified the full data flow: WorldSnapshotService populates loadAvailability from demand destination cities, OptionGenerator generates pickup targets (gated by hasCapacity), and Scorer's calculateMoveScore only considers delivery payoff, not pickup opportunity. Confirmed field names across DemandDeckService (demand.resource) vs ResolvedDemand (demand.loadType) for cross-referencing loadAvailability. |
| 2026-02-15 | Strategy Inspector gap analysis — 7 compounds queries | Mapped all 14 derived implementation needs across server and client. Compounds confirmed: no StrategyAudit type exists, no ScoreBreakdown type, no snapshotSummary generator, infeasible options have reason strings but no structured rejection tracking, ExecutionResult exists but lacks per-step logging, bot_turn_audits stores flat action rows (not rich audit JSON), bot:turn-complete emits basic BotTurnResult (no scoring data). Client-side: StrategyInspectorModal was built in prior branch (compounds/204) but deleted; DebugOverlay with backtick toggle exists as integration point. |
| 2026-02-15 | DropLoad implementation — 3 compounds queries | Investigated AI bot pickup/delivery pipeline, LoadService drop/return methods, and AIStrategyEngine executeLoadActions orchestration. Compounds confirmed setLoadInCity/returnLoad/isLoadAvailableAtCity patterns in server LoadService, identified the full Phase 0/1.5 load action flow, and mapped OptionGenerator demand-matching logic for reachability checks. |
| 2026-02-15 | Debug bot stuck at money=0 — 2 compounds queries | Investigated WorldSnapshotService loadAvailability population. Compounds immediately showed `getAvailableLoadsForCity` is called for `citiesOfInterest` (demand destinations + source cities), confirming load data was correct and the hard reachability gate in OptionGenerator was the root cause. |
| 2026-02-16 | LLM-as-Strategy-Brain PRD analysis — 6 compounds queries | Validated pipeline architecture claims: confirmed Scorer is called 5 times per turn (Phase 0 deliveries, Phase 0 drops, Phase 0 pickups, Phase 1 movement, Phase 2 building), WorldSnapshot lacks opponent money/position/loads/trainType (PRD assumes this exists for Medium/Hard), FeasibleOption uses `action` field not `type` field (PRD uses wrong field name in guardrails). Traced full data flow and integration points. |
| 2026-02-16 | PRD gap analysis — 18 compounds search queries | Systematic search across all 8 known issues: multi-delivery sequencing, scoring constants, drop load logic, state continuity, build oscillation, upgrade timing, discard hand scoring, victory tracking. Also searched for test infrastructure, bot-vs-bot loop, BotConfig, and load service. Compounds mapped the full component graph for each issue area, identifying what exists (AIStrategyEngine phases, Scorer constants, OptionGenerator chains, VictoryService BFS) and what's missing (no bot-side victory check, no state persistence between turns, no automated test harness, hard-coded discard score). |

| 2026-02-18 | ai-v6.2 full implementation — 16 tasks via Compounds workflow | Used `create_project`, `upload` PRD, `spec_project` (wrote technical-spec-v6.2.md), `upload` tech spec, `plan_project` (16 tasks auto-generated), then `get_project`/`update_task` to track all 16 tasks through implementation. Team of 3 agents for Wave 1 (data model). Implemented Plan-Then-Execute architecture: DeliveryPlan interface, PlanExecutor service with 6-phase state machine, LLM plan selection via serializePlanSelectionPrompt, plan resolution in AIStrategyEngine, parameterized loyalty factor in OptionGenerator, ResponseParser.parsePlanSelection. Created 21 PlanExecutor unit tests (all pass). Fixed BotMemoryState in 4 test helpers. All 16/16 tasks marked DONE. |

## 2026-02-14 — Strategy Inspector Tech Spec Research

### Commands Run (11 total)
1. `compounds summary` — project overview: languages, frameworks, entity counts
2. `compounds dependencies --scope external` — 77 external packages mapped
3. `compounds search "debug overlay bot turn" --show-code` — found DebugOverlay class, renderBotTurnSection, socket event handlers
4. `compounds search "StrategyAudit bot turn audit" --show-code` — found BotTurnResult interface, takeTurn orchestration, audit INSERT patterns
5. `compounds search "bot turn complete socket event" --show-code` — found emitTurnChange, bot:turn-start/complete events, BotTurnTrigger flow
6. `compounds search "OptionGenerator feasible infeasible options" --show-code` — found generate(), makeFeasible/makeInfeasible, all option generation methods
7. `compounds search "Scorer scoring dimensions weights" --show-code` — found score(), calculateBuildTrackScore, calculateMoveScore, scoring constants
8. `compounds search "TurnExecutor execution results" --show-code` — found ExecutionResult interface, execute() dispatch, audit INSERT pattern
9. `compounds search "AIStrategyEngine strategy pipeline" --show-code` — found takeTurn 4-phase pipeline, retry logic, PassTurn fallback
10. `compounds search "modal overlay React Phaser UI component" --show-code` — found Phaser modal pattern (rexUI sizers), Radix AlertDialog, DOM overlay
11. `compounds search "WorldSnapshotService snapshot summary" --show-code` — found capture(), WorldSnapshot interface, parseSegments()

### Benefit
Comprehensive cross-module architectural mapping for tech spec. Compounds provided dependency graphs, code flow analysis, and semantic search across 3700+ entities that would have required dozens of manual grep/file-read operations. The AI-generated summaries for each query correctly identified component relationships and data flow patterns.

## 2026-02-16 — Bug Diagnosis: DropLoad Oscillation Loop

### Context
User reported bot picking up Wine at Bordeaux, carrying to Paris, dropping without payment, looping. Invoked compounds skill to search for Phase 0/Phase 1.5 auto-execute logic in AIStrategyEngine. Context compacted mid-search; continued with direct file reads.

### Benefit
Compounds skill was invoked for initial architecture search of the auto-execute load action pipeline. Direct file reads were then used for detailed tracing of the 3-way bug interaction (OptionGenerator + Scorer + AIStrategyEngine).

## 2026-02-16 — Bug Diagnosis: Bot Over-building & Luxembourg Targeting

### Context
User reported bot building to Luxembourg (no reason), orphan Channel tracks trying to reach Aberdeen, and running out of money. Invoked compounds skill with 4 search queries.

### Commands Run
1. `compounds search "OptionGenerator BuildTrack target positions for computeBuildSegments"` — Found generateBuildTrackOptions, extractBuildTargets, extractTrackEndpoints, TrackBuildOptions
2. `compounds search "how BuildTrack options determine which city to build toward"` — Found trackedCityKey, generateBuildTrackOptions full flow
3. `compounds search "identifyTargetCity function"` — Found identifyTargetCity labels by last segment endpoint, not chain target
4. `compounds search "Scorer scoreBuildTrack scoring"` — Found calculateBuildTrackScore formula: BASE(10) + segments*1 - cost + CITY_REACH(5) + chainScore*20
5. `compounds search "AIStrategyEngine Phase 2 build selection"` — Confirmed try-in-score-order with validation
6. `compounds search "rankDemandChains pickupTargets deliveryTargets"` — Found pickupTargets includes ALL cities with matching load (not just primary source)

### Benefit
Compounds mapped the full build targeting pipeline across 4 files (OptionGenerator, computeBuildSegments, Scorer, AIStrategyEngine) and revealed 3 root causes: (1) extractSegments contiguity bug causing high-scored options to fail validation, (2) identifyTargetCity mislabeling when budget insufficient to reach actual target, (3) no minimum build threshold allowing $1M wasteful stubs.

## 2026-02-16 — Bug Diagnosis: Bot Commits to Unaffordable Oslo Route

### Context
Bot picked up Tourists at London and committed all resources to building toward Oslo (30M delivery). This was unaffordable with 37M cash. Used 3 compounds queries to investigate the hasLoad budget penalty gap and Phase 0 pickup eagerness.

### Commands Run
1. `compounds search "rankDemandChains hasLoad budget penalty chainScore carried load"` — Confirmed rankDemandChains architecture, identified hasLoad scoring path
2. `compounds search "Phase 0 pickup load decision before movement executeLoadActions"` — Mapped executeLoadActions→generatePickupOptions→Scorer pipeline for pre-movement pickups
3. `compounds search "generatePickupOptions reachability gate aspirational pickup"` — Found pickup scoring with reachability soft-gate (0.15x penalty) but no affordability check

### Benefit
Compounds confirmed two root causes: (1) hasLoad branch in rankDemandChains has NO budget penalty (comment says "must deliver them"), causing Tourists→Oslo to score 5.0 and dominate all chains; (2) Phase 0 picks up loads matching any demand card without checking if delivery is affordable.

## 2026-02-19 — AI Pipeline Architecture Discovery (16 queries)

**Task:** Comprehensive architectural discovery of the AI bot pipeline for tech spec to replace OptionGenerator + Scorer with ContextBuilder + LLM + ActionResolver pipeline.

**Queries run:**
1. `compounds summary` — project overview (4306 entities, 99% TypeScript)
2. `compounds search "AIStrategyEngine takeTurn pipeline orchestration"` — discovered full pipeline: Phase 0 → Plan Resolution → Phase 1 (Movement) → Phase 2 (Build), with plan-then-execute architecture
3. `compounds search "WorldSnapshot capture game state"` — discovered capture() function, WorldSnapshot interface, DB query joining games/players/player_tracks
4. `compounds search "TurnExecutor execute action dispatch"` — discovered action switch dispatch (BuildTrack, MoveTrain, PickupLoad, DeliverLoad, DropLoad, UpgradeTrain, DiscardHand, PassTurn)
5. `compounds search "GuardrailEnforcer check safety rules"` — discovered it's a no-op; returns unchanged selections; PlanValidator handles actual rule enforcement
6. `compounds search "LLMStrategyBrain decide select action LLM"` — discovered full LLM pipeline: serialize → chat → parse → guardrail, with retry chain (full → minimal → heuristic)
7. `compounds search "BotTurnTrigger turn scheduling lifecycle"` — discovered turn trigger with pendingBotTurns guard, human connectivity check, housekeeping queries
8. `compounds search "computeBuildSegments Dijkstra pathfinding segments"` — discovered multi-source Dijkstra with budget constraints, target-aware path selection
9. `compounds search "TrackNetworkService findPath buildTrackNetwork isConnected"` — discovered A* pathfinding, string-keyed graph, ferry edge support
10. `compounds search "trackUsageFees computeTrackUsageForMove"` — discovered union track graph, preferOwnTrackPath, edge ownership tracking
11. `compounds search "LoadService isLoadAvailableAtCity getAvailableLoads"` — discovered singleton LoadService with loadConfiguration map, getSourceCitiesForLoad
12. `compounds search "TrackBuildingService addPlayerTrack isValidConnection"` — discovered Result<T,E> pattern, city connection limits, ferry port handling
13. `compounds search "PlayerService moveTrainForUser deliverLoadForUser"` — discovered server-authoritative PlayerService with transactional DB ops
14. `compounds search "DemandDeckService drawCard discardCard demand"` — discovered dual client/server implementations, draw/discard pile management
15. `compounds search "GameStateSerializer serialize prompt context"` — discovered structured prompt generation with skill-level scaling, chain analysis
16. `compounds search "ResponseParser parse LLM response intent"` — discovered JSON + regex fallback parsing, index validation, ParseError handling
17. `compounds search "OptionGenerator generate FeasibleOption BuildTrack MoveTrain"` — discovered generate() orchestrating multiple option types
18. `compounds search "PlanExecutor executePlan plan phase movement build"` — discovered stateless plan executor with phase flow
19. `compounds search "Scorer score heuristic rank options"` — discovered score-and-sort with per-action-type heuristics

**Benefit:** Compounds provided complete architectural understanding across 23 source files in the AI pipeline, including cross-module dependencies, method signatures, type definitions, and execution flow. This would have required reading ~5000 lines of code manually. The semantic search surfaced exactly the right components for each query, including files I wouldn't have found with grep (e.g., connectedMajorCities.ts for buildTrackGraph, MapTopology.ts for grid functions).

## 2026-02-20 — Session continuation: Guardrail 4 feasibility tests

**Context:** Continued from previous session. Guardrail 4 code (block BUILD toward unaffordable targets) was already written. This continuation focused on writing tests and running the full test suite.

**Compounds usage in this continuation:** N/A — code changes were scoped to test file only, using existing knowledge from prior session's compounds queries.

**Result:** 7 new tests for Guardrail 4, all 27 GuardrailEnforcer tests pass, clean TypeScript build.

## 2026-02-21 — Initial build strategic blindness investigation

**Context:** User reported bot starting at Ruhr instead of Holland→Berlin corridor despite having Cheese+Flowers demands to Berlin. Compounds used to trace initial build context pipeline.

**Commands run:**
1. `compounds search "initial build phase cold start city selection strategy"` — found buildOrderedCandidates in AIStrategyEngine, identifyTargetCity in OptionGenerator, CitySelectionManager client-side
2. `compounds search "system prompt initial build instructions strategy guidance"` — found CRITICAL RULE 10 ("prefer central Europe") and full system prompt structure
3. `compounds search "initial build cold start estimateTrackCost zero segments empty"` — confirmed estimateTrackCost returns 0 when no segments exist

**Benefit:** Compounds identified the root cause: during initialBuild with no track, estimateTrackCost returns 0 for ALL cities (the "can't estimate without frontier" early return). This means the LLM sees `~0M track needed` for every demand, giving no distance signal. Combined with the system prompt only saying "prefer central Europe" without showing which demands align, the LLM picks Ruhr for a 48M Tourist demand instead of the obvious Holland→Berlin corridor.
2026-02-22 08:47 — Used compounds search to trace bot starting city + initial build turn flow. Identified AIStrategyEngine.takeTurn → autoPlaceBot, ContextBuilder.isInitialBuild, ActionResolver.heuristicFallback, computeBuildSegments cold-start path. Compounds correctly surfaced all 5 key components and their relationships.
2026-02-22 — Used 3 compounds searches to investigate turn workflow enforcement for JIRA-3 documentation. (1) `search "PlanExecutor execute phase selection build travel act chainArrivalAction appendBuildStep"` mapped the 3-enhancement architecture and phase selection logic, (2) `search "turn workflow enforcement move deliver pickup build single turn"` confirmed no systematic workflow enforcer exists, (3) `search "ActionResolver resolveMultiAction combine actions"` traced MultiAction resolution and callers. Compounds confirmed the gap: system uses ad-hoc chaining, not a proper workflow. Informed the TurnComposer design in jira-3-turnWorkflowEnforcer.md.
2026-02-22 — Used compounds search to investigate whether LLM receives victory context. `search "LLM system prompt victory conditions winning 250M major cities connect"` found VictoryService (client), getConnectedMajorCityCount (server), and confirmed victory logic exists but is NOT surfaced to the LLM beyond a static rule line and city count. Direct file reads then confirmed ContextBuilder.serializePrompt shows "Major cities connected: X/8" but no missing-city list, cost estimates, or phase-appropriate directives. Informed JIRA-4 design.
2026-02-22 — Used 3 compounds searches for JIRA-5 (route planning context enrichment). (1) `search "geographic proximity between demands corridor overlap route combination clustering"` found the old `demandProximityBonus` (Scorer.ts) and `rankDemandChains` (OptionGenerator.ts) — both deleted in v6.3 refactor, confirming geographic clustering was lost. (2) `search "detour cost route overlap shared track between demands"` found `estimateDistance`, `calculateNewSegmentCost`, `isAdjacentToNetwork` — infrastructure exists but is not surfaced to the LLM prompt. (3) `search "route planning prompt context what information does the LLM receive"` — index stale (returned deleted files), but confirmed no route-specific prompt serialization exists. Key finding: old pipeline had geographic intelligence that wasn't carried to LLM-based routing.
2026-02-24 — Used compounds search "PlanExecutor advanceStop route completion currentStopIndex" to map PlanExecutor architecture for bot turn log bug analysis. Compounds returned detailed explanation of advanceStop/routeCompleted/executeTravelPhase/advancePhase/chainArrivalAction relationships and identified the 5 key components in PlanExecutor.ts. Combined with direct file reads of PlanExecutor.ts, ActionResolver.ts, AIStrategyEngine.ts, GuardrailEnforcer.ts, majorCityGroups.ts, and GameTypes.ts to trace 4 bugs: (1) initialBuild PassTurn when all stops reachable, (2) route "completed" without delivery due to advanceStop overshoot, (3) chainArrivalAction false arrival detection (snapshot not updated), (4) appendBuildStep draining money toward unrelated cities.
2026-02-24 — JIRA-3 TurnComposer tech spec research: 13 compounds commands (agent-prompt cli-usage, status, summary, dependencies --scope external/internal, 10 semantic searches). Compounds summary confirmed 4337 entities indexed. Semantic searches mapped: (1) PlanExecutor chain/multi-action architecture with 5 components analyzed, (2) TurnPlan/ActionResolver composition with resolveMultiAction sequential state simulation, (3) build track budget pipeline (estimateTrackCost, addPlayerTrack, isWithinTurnBudget, getBuildBudget), (4) pickup/deliver during move flow (LoadService, handleLoadPickup, resolveDeliver), (5) AIStrategyEngine 6-stage pipeline with decision gate integration point, (6) GuardrailEnforcer checkPlan 3-rule enforcement, (7) applyPlanToState/cloneSnapshot cumulative state simulation, (8) ContextBuilder GameContext build orchestration, (9) WorldSnapshot bot state capture, (10) StrategicRoute active route planning. All 10 searches returned relevant components with source code. Combined with direct file reads of PlanExecutor.ts (524 lines), ActionResolver.ts (942 lines), AIStrategyEngine.ts (457 lines), ContextBuilder.ts (1000+ lines), GuardrailEnforcer.ts (149 lines), GameTypes.ts (type signatures), PlanExecutor.test.ts (1020 lines).
2026-02-24 — JIRA-3 TurnComposer implementation finalization: Used Compounds MCP tools (get_all_projects, get_project, get_task_with_context ×7, update_task ×7, update_project) to orchestrate the full project completion workflow. Retrieved all 7 task contexts in parallel to assess implementation status — discovered 5 of 7 tasks were already implemented, only TEST-002 (PlanExecutor test cleanup) and INF-002 (build+test verification) remained. Created team "turncomposer-finalize", dispatched test-cleaner agent for TEST-002 (removed 4 obsolete multi-action test blocks, ~590 lines deleted, 17 tests passing). Ran INF-002 myself: fixed 9 type errors in AIStrategyEngine.test.ts (route action casing, phase literals, StrategicRoute property mismatches), added TurnComposer passthrough mock. Final result: server build clean, 331 AI tests passing across 11 suites. All 7 tasks marked DONE, project marked COMPLETED via Compounds.
2026-02-24 — JIRA-6 RouteValidator implementation: Compounds was used in prior session for ContextBuilder search to map DemandContext fields. This continuation session focused on implementing and testing the RouteValidator and LLMStrategyBrain integration using direct file reads (all files already identified in prior compounds queries). Created RouteValidator.ts (289 lines), integrated into LLMStrategyBrain.planRoute(), wrote 17 unit tests + 4 integration tests. All 353 AI tests pass.
2026-02-25 — Bot behavior analysis (Roma build, incomplete turns, no discard): Used compounds search to investigate 3 bot issues: (1) "heuristic fallback build target selection when LLM route planning fails" mapped heuristicFallback → resolveBuild → computeBuildSegments → findDemandBuildTarget pipeline, (2) "TurnComposer multi-action turn move deliver pickup build combination" mapped TurnComposer.compose → splitMoveForOpportunities → tryAppendBuild architecture, (3) "discard hand demand cards option when no viable routes available" confirmed DiscardHand only exists in LLM path, not heuristic fallback. Direct file reads of AIStrategyEngine.ts, TurnComposer.ts, PlanExecutor.ts, ActionResolver.ts, LLMStrategyBrain.ts, GuardrailEnforcer.ts identified 3 root causes: speculative build toward cheapest demand without feasibility check, TurnComposer Phase A not enriching after route completion, and heuristicFallback having no DiscardHand step (BUILD→PASS only).
2026-02-25 — JIRA-5 route planning context enrichment (BE-001 through BE-007): Used compounds agent-prompt cli-usage for CLI reference, then compounds search "DemandContext interface and computeDemandContext method" (filtered to GameTypes.ts) and compounds search "ContextBuilder computeDemandContext isLoadRuntimeAvailable" (filtered to ContextBuilder.ts) to map the DemandContext type, carried-count logic, and computeDemandContext flow before implementing. Compounds correctly identified all relevant components (DemandContext, isLoadRuntimeAvailable, getLoadTotalCopies, computeDemandContext, findBestSupplyCity, isCityOnNetwork) and their relationships. Direct file reads then used for precise line-level implementation. All 105 AI tests pass (74 ContextBuilder + 15 AIStrategyEngine + 16 LLMStrategyBrain).
2026-02-25 — Gemini Provider + Difficulty-Based Model Selection: Used compounds-dev MCP tools for full project lifecycle (create_project, init_repo, spec_project, generate_tech_spec, plan_project, implement_all_tasks, implement_task ×8, implement_task_finalize ×16, complete_subtasks ×8). Compounds generated 8 tasks from PRD/tech spec, managed dependency ordering, and tracked progress through all 8 implementations. Also used compounds CLI (5 semantic searches, 1 structural query) in the initial investigation to discover the 95% complete Gemini adapter pattern. All 8 tasks finalized: BE-002 (model defaults), BE-001 (route validation), BE-003 (GoogleAdapter hardening), FE-001 (client API wiring), TEST-001 (GoogleAdapter tests, 11 new), TEST-002 (LLMStrategyBrain provider tests, 5 new), TEST-003 (lobby route integration tests, 5 new), DOC-001 (.env.example update). 63 tests pass across 3 suites.
2026-02-25 — JIRA-5 test writing session: Used compounds query "ContextBuilder class - DemandContext interface, Corridor interface, computeCorridors, detectOnTheWay, countCarriedLoads, serializeRoutePlanningPrompt, formatReachabilityNote, estimatedTurns computation" to map all new JIRA-5 components before writing tests. Compounds returned 956 semantic matches identifying all key entities: computeDemandContext, computeAllDemandContexts, formatReachabilityNote, serializeRoutePlanningPrompt, getLoadTotalCopies, countCarriedLoads, hexDistance, planRoute, DemandContext. This informed test construction with correct hex distances, threshold values, and method signatures. Wrote 56 new ContextBuilder tests (TEST-001 through TEST-004) covering loadChipTotal/loadChipCarried, estimatedTurns, SCARCE/UNAVAILABLE formatting, corridor detection with union-find, on-the-way detection, serializeRoutePlanningPrompt content validation, and build() integration. Plus 4 LLMStrategyBrain integration tests verifying serializeRoutePlanningPrompt is called during planRoute. All 130 ContextBuilder tests and 20 LLMStrategyBrain tests pass.

## 2026-02-27: JIRA-10 LLM Secondary Build Target — Tech Spec Research

**Used compounds for:**
- `compounds status` — confirmed index healthy (4337 entities, consistent)
- `compounds summary` — project overview: TypeScript 99%, 4337 entities, React+Express stack
- `compounds dependencies --scope external` — 77 packages (react, express, pg, phaser, neverthrow, etc.)
- `compounds dependencies --scope internal` — 158 modules, identified key imports (GameTypes 113 imports, MapTopology 10 imports, computeBuildSegments 4 imports, ContextBuilder 4 imports, PlanExecutor 3 imports, LLMStrategyBrain 3 imports)
- `compounds search "findDemandBuildTarget"` — confirmed function in PlanExecutor.ts:441, identified call chain: findNextBuildTarget → findDemandBuildTarget → appendBuildStep → resolveBuild
- `compounds search "secondaryBuildTarget"` — confirmed NO existing code for secondary build targets. The concept is entirely new.
- `compounds search "serializeRoutePlanningPrompt"` — found the prompt serialization in ContextBuilder.ts
- `compounds search "StrategicRoute type definition"` — found type at GameTypes.ts:407, confirmed no secondaryBuildTarget field
- `compounds search "tryAppendBuild"` — found in TurnComposer.ts, identified the fallback chain: route stops → victory cities → demand cities
- `compounds search "PlanExecutor build target"` — confirmed executeInitialBuild → findInitialBuildTarget → findDemandBuildTarget flow
- `compounds search "TurnComposer build"` — identified Phase B build enrichment pattern
- `compounds search "RouteValidator"` — confirmed RouteValidator.ts exists with per-stop + cumulative budget checks
- `compounds search "LLMStrategyBrain parse route"` — found planRoute method flow: prompt → LLM → parseStrategicRoute → RouteValidator.validate
- `compounds search "computeBuildSegments"` — confirmed multi-source Dijkstra with target-aware path selection
- `compounds search "MapTopology nearby cities"` — found hexDistance, getHexNeighbors, and city computation methods

**Benefit:** Compounds provided semantic understanding of the full build-target pipeline across 7+ files, showing how findDemandBuildTarget is the greedy heuristic fallback, how TurnComposer.tryAppendBuild delegates to it, and confirming that secondaryBuildTarget is entirely absent from the codebase. This would have taken significantly longer with grep alone due to the cross-module relationships.

## 2026-02-28 — JIRA-12 Codebase Profile Investigation

### Compounds used for:
1. `compounds auth status` — verified authentication (authenticated, 59m remaining)
2. `compounds status` — verified index health (4337 entities, consistent graph/vector)
3. `compounds summary` — got project overview (TypeScript 99%, server/client/shared architecture)
4. `compounds dependencies` — identified 77 packages (React, Express, pg, Phaser, neverthrow, uuid)
5. `compounds search "ferry movement"` — found ferry handling components (TrainMovementManager, GameScene, ContextBuilder.isFerryOnRoute)
6. `compounds search "ferry port"` — identified FerryPort, FerryConnection, ferryConnections config
7. `compounds search "half speed"` — found getBotSpeed in ActionResolver, advanceStop in PlanExecutor
8. `compounds search "DropLoad"` — found TurnExecutor.handleDropLoad, loadService methods
9. `compounds search "ActionResolver"` — mapped full action dispatch tree (resolve → resolveSingleAction → resolveX)
10. `compounds search "TurnPlan"` — identified TurnPlan union type, executePlan, planToOption
11. `compounds search "pickup load"` — traced full pickup flow (client → server → AI pipeline)
12. `compounds search "canPickup"` — found ContextBuilder.computeCanPickup, GuardrailEnforcer.bestPickup
13. `compounds search "feasibility"` — mapped feasibility checking (FeasibleOption, computeCanDeliver, checkPlan)
14. `compounds search "GuardrailEnforcer"` — understood guardrail enforcement (4 rules, checkPlan flow)
15. `compounds search "discard hand"` — traced discard flow (handleDiscardHand, discardHandForUser, resolveDiscard)
16. `compounds search "TurnComposer"` — identified turn composition layer
17. `compounds search "OptionGenerator"` — found planToOption, FeasibleOption, LoggedOption
18. `compounds search "LLMStrategyBrain"` — mapped LLM decision pipeline (decideAction, planRoute, createAdapter)
19. `compounds search "AIStrategyEngine"` — understood orchestrator (takeTurn 6-stage pipeline)
20. `compounds search "DiscardHand"` — traced DiscardHand action type through pipeline

### Benefit:
Compounds provided semantic search and dependency graphs that quickly identified the full AI pipeline architecture, cross-module relationships between 15+ services, and the exact code paths for all 10 bugs. The vector-indexed search was especially valuable for understanding how ferry handling, load management, and guardrail enforcement interact across ContextBuilder → LLMStrategyBrain → ActionResolver → GuardrailEnforcer → TurnComposer → TurnExecutor.

---

## 2025-03-03 — JIRA-19: FE-003 + Jest Infrastructure (Session 3)

### Tasks Completed:
- FE-003: Display LLM metadata (model, latency, tokens, retried) in DebugOverlay
- Jest Infrastructure: Refine mock management with clearMocks config

### Compounds MCP Usage:
- `implement_task` / `implement_all_tasks` / `implement_task_finalize` workflow for both tasks
- `complete_subtasks` for batch subtask completion
- `compounds agent-prompt cli-usage` for CLI reference

### Benefit:
Compounds MCP orchestrated the full task lifecycle including acceptance criteria validation, subtask tracking, and auto-proceed mode for sequential task execution. The implement_task_finalize workflow enforced quality gates (validation summary, criteria counts, critical findings) before marking tasks done.

---

## 2026-03-03 — JIRA-20: Movement Budget Bug (continued session — tasks 4-6)

### Context:
Continued implementation of JIRA-20 project (943ee520) from a prior session that ran out of context. Completed tasks TEST-002, BE-003, and TEST-003.

### Compounds MCP Usage:
- `get_project_status` to verify project state (3 DONE, 1 IN_PROGRESS, 2 TODO)
- `implement_all_tasks` for progress sync between tasks (3 calls)
- `implement_task` to get next task context (3 calls)
- `implement_task_finalize(phase="validate")` + `implement_task_finalize(phase="mark_done")` for each task (3 pairs)
- `complete_subtasks` for batch subtask completion (3 calls)

### Benefit:
Compounds MCP maintained project state across a session boundary — the tracking file correctly identified which tasks were done vs remaining. Auto-proceed mode enabled seamless sequential execution of the final 3 tasks without manual intervention. The finalization workflow enforced quality gates ensuring all acceptance criteria were validated with evidence before marking done.

## 2026-03-04 — Session 3: BE-003 + TEST-001 completion (continued from session 2)

### Context:
Continued implementing tasks for project "Bot Safety Nets" (025751be). Completed BE-003 (dead-end milepost pruning in computeBuildSegments Dijkstra) and TEST-001 (verify all test coverage).

### Tools Used:
- `implement_task_finalize(phase="validate")` + `implement_task_finalize(phase="mark_done")` for BE-003 and TEST-001
- `complete_subtasks` for batch subtask completion (2 calls, 10 subtasks total)
- `implement_all_tasks` for progress sync and next-task routing
- `implement_task` for TEST-001 task retrieval
- `get_task` for full subtask ID retrieval (session summary had truncated IDs)

### Benefit:
Compounds tracked project state correctly across an auth expiration + MCP reconnect mid-session. The validate→mark_done two-phase finalization caught the session state reset (required re-running validate after re-auth). TEST-001 was correctly identified as already complete from inline test creation during BE-001/BE-002/BE-003, avoiding duplicate work. All 4/4 tasks finalized successfully.

## 2026-03-05: JIRA-31 LLM Response Log — Session 2 (continuation)

### Context:
Continuing JIRA-31 implementation after context compaction. Resumed from FE-001 finalization through FE-002 and TEST-001.

### Compounds Tools Used:
- `implement_task_finalize` (validate + mark_done) for FE-001, FE-002, TEST-001
- `implement_all_tasks` for task progression sync (3 calls)
- `implement_task` to get FE-002 and TEST-001 task prompts
- `get_task_with_context` for FE-001 acceptance criteria review
- `complete_subtasks` for FE-001 (4), FE-002 (5), TEST-001 (6) subtasks

### Benefit:
Compounds maintained correct project state across a context compaction boundary. The implement_all_tasks orchestrator correctly tracked 6/8→7/8→8/8 completion. Task prompts provided precise acceptance criteria and subtask IDs enabling targeted implementation. The validate→mark_done finalization workflow enforced evidence-based review before marking tasks done.

## 2026-03-06: JIRA-42 Guardrail Overhaul (Standard tier)

### Compounds Tools Used:
- `plan_change` for JIRA-42 spec generation and project creation
- `create_project`, `add_task` (x2) for project setup
- `implement_all_tasks` for dependency-ordered execution
- `implement_task` (x2) for task retrieval
- `update_task` (x2) for marking tasks DONE
- `compounds upload` CLI (x3) for spec and task prompts

### Benefit:
Compounds structured a 2-task project (code changes + test overhaul) from a JIRA ticket. Task prompts provided precise file:line references and acceptance criteria. The implement_all_tasks orchestrator correctly ordered code-first, tests-second. Note: `implement_task_finalize` session state did not persist across calls, requiring fallback to `update_task`.

---

## 2026-03-06: JIRA-40 — Terrain-Aware Track Cost Estimation

### Tools Used:
- `compounds search` (x3) for discovering estimateTrackCost, scoreDemand, computeBuildSegments, hexDistance, getTerrainCost and their relationships
- `compounds impact estimateTrackCost --direction both --max-depth 3` — mapped 14 affected entities across 3 depth levels
- `compounds index .` for incremental reindex
- `plan_change`, `gen_spec`, `create_project`, `add_task`, `implement_task`, `update_task`
- `compounds upload` CLI (x2) for spec and task prompt
- `get_reference_architecture_context`, `pattern_detection`, `get_testing_frameworks`

### Benefit:
Compounds search revealed that `computeBuildSegments.ts` already had a complete Dijkstra implementation with terrain-aware costs — this was the key architectural insight that shaped the solution (reuse the pattern rather than inventing a new approach). Impact analysis confirmed the change was safe: 5 direct callers all use the same interface, no signature change needed. The `compounds search` explanation summaries provided a clear map of how estimateTrackCost, scoreDemand, computeBuildSegments, and getTerrainCost relate. Same `implement_task_finalize` session state issue — used `update_task` fallback.
| 2026-03-06 | Research: toast/banner notification system architecture | Compounds `index .` updated 185 new entities. Six semantic searches revealed: (1) existing toast system in UIManager.showHandToast and TurnNotification, (2) Toaster/sonner component in lobby, (3) GameScene socket listeners for bot:turn-complete and state:patch, (4) full bot:turn-complete payload structure with loadsDelivered/loadsPickedUp, (5) UI depth layering conventions (2500 for toasts, 3000 for modals, 10000 for TurnNotification, 100000 for LoadsReferencePanel), (6) DebugOverlay is HTML-based DOM overlay not Phaser. Compounds structural query on GameScene listed all 25 methods. Impact/flows commands did not resolve the class entity but the semantic searches compensated fully. |
| 2026-03-07 | JIRA-54: Dead hand discard in heuristic fallback | Compounds search surfaced heuristicFallback structure (5-step priority chain), DemandContext fields (isSupplyOnNetwork, isDeliveryOnNetwork, isLoadOnTrain, estimatedTrackCost*), ContextBuilder methods (computeCanDeliver, computeReachableCities, isCityOnNetwork), and scoreDemand's existing affordability penalty. Impact analysis on heuristicFallback confirmed 10 direct callers all within ActionResolver — isolated leaf method, safe to modify. Pattern detection and testing frameworks loaded for spec compliance. Full plan_change → gen_spec → implement_task workflow completed. Same implement_task_finalize session state issue — used update_task fallback. |
| 2026-03-10 | Validate upgrade advice gate fix | Used compounds to validate that gating upgrade advice in serializePrompt is sufficient. Compounds search + structural query on getSystemPrompt traced all upgrade context paths: (1) dynamic user prompt advice in serializePrompt (gated by my fix), (2) strong nudge at top of serializePrompt (already had stricter gate), (3) static system prompt TRAIN UPGRADE STRATEGY section (lines 189-205, always included). Compounds revealed the gap: system prompt still contains upgrade instructions, but user confirmed the dynamic gate is sufficient since system prompt already says "DON'T upgrade before first delivery" and "DON'T upgrade with <30M". |
| 2026-03-10 | JIRA-82/83/84 planning: phantom carried load, A2 movement waste, reEvaluateRoute trigger | Compounds search found `computeSingleSupplyDemandContext` isLoadOnTrain logic, `findMoveTargets` A2 chain code, and confirmed `reEvaluateRoute` is only called from `AIStrategyEngine.takeTurn`. Structural query `compounds query reEvaluateRoute -r calls` mapped the call graph. Reference architecture and pattern detection loaded for spec writing. |
